doctype html
html(lang="en")
    head
        meta(charset="UTF-8")
        meta(name="viewport", content="width=device-width, initial-scale=1.0")
        title #{nombreSitio}#{pagina ? ' | ' + pagina : ''}
        link(rel="stylesheet", href="/css/app.css")
    body
        include header
        block contenido
        include footer