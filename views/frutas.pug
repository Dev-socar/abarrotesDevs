extends ./templates/template

block contenido 
    main(class='w-11/12 mt-16 mx-auto mb-0')
        h2(class='text-center text-3xl md:text-5xl') Las Frutas Mas Frescas del Condado
        if articulos.length === 0
            h2(class="text-center text-xl md:text-3xl text-gray-400 mt-5") No existen Frutas Aun
        else
            div(class='grid md:grid-col-4 lg:grid-cols-6 gap-3 mt-5')
                include ./templates/cards